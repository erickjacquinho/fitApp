// gray-500 token — used where Tailwind className cannot be applied (TextInput placeholderTextColor, Icon color prop)
export const COLORS = {
  placeholder: '#6B7280',
  iconMuted: '#6B7280',
} as const;
