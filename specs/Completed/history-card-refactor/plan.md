# Technical Plan
- File: `src/features/training/components/HistorySessionCard.tsx`
- Modificar o JSX renderizado para usar `BaseCardList` com flex-wrap e ícones. 
- Atualizar importações para incluir `Calendar` e `Clock` do `lucide-react-native`.
- Remover `programName` e o render condicional de `completedSets/totalSets`.
- Aplicar tokens NativeWind do FitApp (gap-1.5, text-secondary, flex-row).
