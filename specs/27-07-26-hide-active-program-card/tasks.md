# Tasks: Dynamic Active Program Card Filtering

- [x] T001 [skill: ui-ux-pro-max] Refactor `TrainingHomeScreen.tsx` to compute `visiblePinnedPrograms` and `visibleOtherPrograms` filtering out the active session's program ID (`activeSession?.programId`).
- [x] T002 [skill: general] Run `npx tsc --noEmit` validation gate to ensure zero TypeScript errors.
